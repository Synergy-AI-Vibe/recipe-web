# Copy this file to .env.local and fill in real values.
# Required when calling the API: absolute http(s) URL, e.g. http://localhost:8080/api
# Public value, embedded at build time. Never put credentials here.
NEXT_PUBLIC_API_BASE_URL=
